# dispatch snapshot 印 gate の enrollment marker（scribe・s2 台帳）
# 置いた日 = 2026-09-07 / 置いた席 = s2 admin / 出自 = bd s2-07l.1
#
# 何を守るか: **stamp 後・dispatch 前に acceptance が書き換わっていないこと**。
#   読むもの … notes の `[ORCH-DISPATCH-ACCEPTANCE-SNAPSHOT v1]` 印（acceptance の sha256）
#   判定 ……… scripts/scribe-dispatch-snapshot.py check
#   deny …… spawn せず **exit 14**（intake QA deny の exit 12 とは別番号）
#   免除 …… `created_at < SNAPSHOT_EPOCH=2026-08-24T00:00:00Z` は legacy 通過。
#            s2 台帳の bead は全件 EPOCH より後ゆえ、この経路で抜けるものは無い。
#
# なぜ置くか（発見の経緯）: intake-qa-gate を置いた直後の dry-run で
#   `[plan] dispatch snapshot: snapshot=not-enrolled` が出た。
#   admin は leg 0 で snapshot 印を焼いていたが、**それを検証する門が arm されていなかった**
#   ＝焼いた印が飾りになっていた。「印を焼いた」は「門が効いている」の証拠にならない
#   （逆向きの取り違え = 「印なしで通った」を「門が無い」と読むこと。両方とも同じ誤り方）。
#
#   snapshot-mismatch は承認体制の fail-closed トリガ 2 つのうちの 1 つ（3 クラス該当と並ぶ）で、
#   docs/protocol.md の merge gate block が「該当したら AI の主観に関係なく止める」と定めている。
#   門が arm されていない台帳では、その裁定が機械の側に存在しないことになる。
